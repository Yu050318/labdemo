# LabFlow P0 冻结基线

> 版本：P0-Freeze-1  
> 冻结日期：2026-07-29  
> 状态：允许设计部、开发部、测试部进入 G1/G2  
> 上位文档：`LABFLOW_PRD_AND_TECH_STACK.md` V1.2  
> 执行计划：`docs/superpowers/plans/2026-07-29-labflow-cross-department-execution.md`

## 1. P0 产品目标

面向个人实验人员交付响应式 Web 应用，完成以下闭环：

```text
安排实验
→ 关联或手工创建实验方案
→ 勾选器材与前置准备
→ 按步骤执行实验
→ 使用多计时器和提醒
→ 确认完成并归档
→ 查看当日总结与次日任务
```

P0 只开放个人空间，但数据和权限边界必须为后续团队空间预留扩展能力。

## 2. P0 范围

### 2.1 必须交付

1. 个人账户、登录、退出、找回密码和基础偏好。
2. 按日期及早、中、晚安排实验任务。
3. 手工创建、编辑、搜索和关联实验方案。
4. 实验器材、试剂、材料和前置准备清单。
5. 实验步骤勾选、跳过、备注及进度展示。
6. 可同时运行的多个计时器。
7. Web Push、站内提醒和错过提醒补显。
8. 实验完成确认、执行记录和历史归档。
9. 每日晚间汇总当日任务和次日任务。
10. 短时离线读取、勾选和恢复。
11. JSON/CSV 数据导出。
12. 桌面端和移动端响应式体验。

### 2.2 明确不包含

- 文件上传与文档处理。
- PDF、OCR、AI 方案提取。
- 联网文献检索、三来源比较和 RAG。
- Supabase Queue、pgvector 和 Python Worker。
- Docker、云端计算 Worker 和桌面安装程序。
- 材料库存管理、仪器/场地预约。
- Zotero 集成。
- 团队成员、角色和协作空间。
- 危险实验的闹钟级安全保证。

上述内容统一进入 P1 或 P2，不得在 P0 实施过程中顺带加入。

## 3. P0 业务规则冻结

### 3.1 提醒服务等级

- P0 提供“尽力分钟级发送 + 站内补显”，不承诺指定秒必达。
- 到点提醒不自动将步骤或实验标记为完成。
- Push 被拒绝、失效、设备离线或浏览器限制时，提醒仍写入站内消息。
- 用户再次打开应用时，应看到未读和错过的提醒。
- 产品界面必须明确提示：LabFlow 不能替代危险实验所需的机构安全制度、专用报警设备或人工值守。

### 3.2 离线范围

P0 的短时离线支持：

- 查看已缓存的当天日程、关联方案、准备清单和实验步骤。
- 勾选或取消勾选准备项与实验步骤，联网后幂等同步。
- 查看已启动计时器的推算剩余时间。

P0 离线时暂不支持：

- 注册、登录和找回密码。
- 创建或编辑日程、实验方案和实验结构。
- 启动、暂停、延长、缩短或删除计时器。
- 完成归档、数据导出和修改账户设置。

离线期间的勾选必须显示“待同步”状态；冲突不得静默覆盖。

### 3.3 浏览器与设备范围

- 桌面端正式支持 Chrome、Edge、Firefox 最新两个稳定大版本。
- macOS 正式支持 Safari 17 及以上版本。
- 移动端正式支持 Android Chrome 最新两个稳定大版本，以及 iOS/iPadOS Safari 17 及以上版本。
- 不支持 Internet Explorer。
- iOS/iPadOS 的 Web Push 需要用户将 Web App 添加到主屏幕并主动授权；未满足条件时使用站内提醒降级。
- 设计与测试至少覆盖一个典型桌面宽度和一个典型手机宽度，具体断点由设计部与开发部共同定义。

### 3.4 登录方式

- P0 使用邮箱和密码注册、登录。
- 注册邮箱需要验证。
- 支持通过邮箱找回密码。
- 社交登录、机构单点登录和 MFA 不属于 P0。
- 所有业务数据必须由 Supabase RLS 按当前用户和个人空间隔离。

### 3.5 数据保留与账户删除

- 正常账户的数据持续保留，直至用户主动删除。
- 普通业务记录采用软删除并保留 30 天，保留期内允许恢复；到期后进入永久删除流程。
- 已完成实验引用的方案版本不得被后续编辑覆盖；用户可停用方案，但不得破坏历史执行记录。
- 用户发起账户删除后进入 7 天撤销期；撤销期结束后开始永久删除，并在 30 天内完成。
- 法律、安全或备份恢复所需的最小记录按平台政策处理，但不得继续用于产品功能展示。

### 3.6 晚间汇总

- 默认在用户当地时间 21:00 生成。
- 用户可在设置中修改汇总时间或关闭汇总。
- 以用户当前设置的 IANA 时区为准。
- 每个用户、每个当地日期最多生成一份当日汇总。
- 用户修改时区后，未来汇总按新时区重新计算；已经生成的当日汇总不重复生成。
- 汇总仅展示状态，不自动顺延、关闭或修改任务。

### 3.7 数据导出

- JSON 导出当前账户内可迁移的 P0 结构化数据，包括日程、方案及版本、清单、步骤执行、计时记录、提醒和汇总。
- CSV 分别导出日程任务、实验方案、实验执行记录三个表格。
- P0 不导出平台内部日志、已永久删除数据或未来 P1 文件。
- 导出文件应包含生成时间和数据版本说明。

### 3.8 部署与访问范围

- 优先验证 Sites 部署；无法满足 Next.js、PWA、Web Push、SSR/API 或环境变量要求时回退 Vercel。
- Supabase 继续承担 PostgreSQL、Auth、RLS 和 Cron。
- 试点阶段只向指定测试用户开放账户。
- 正式发布后网站可公开访问，但个人业务数据必须登录后才能查看。
- Sites 或 Vercel 的公开页面不得泄露任何用户实验数据。

## 4. 核心验收场景

### AC-P0-01：账户隔离

用户 A 无法读取、修改或通过直接请求访问用户 B 的日程、方案、步骤、计时器、提醒和归档记录。

### AC-P0-02：日程安排

用户可以按日期和早、中、晚创建实验任务，并可以编辑、改期、取消和查看状态。

### AC-P0-03：方案关联

用户可以从个人知识库选择已有方案，也可以手工创建方案；实验执行引用明确的方案版本。

### AC-P0-04：准备清单

用户可以逐项勾选器材、试剂、材料和前置准备，刷新或重新登录后状态仍然保留。

### AC-P0-05：步骤执行

用户可以完成、取消完成、跳过和备注步骤；跳过时必须记录原因；总体进度与步骤状态一致。

### AC-P0-06：多计时器

用户可以同时运行多个计时器；页面刷新或换设备后，剩余时间依据目标结束时间恢复；到点不自动完成步骤。

### AC-P0-07：提醒降级

Push 可用时用户收到通知；Push 不可用时系统仍保存站内提醒，用户重新打开应用后可以看到。

### AC-P0-08：离线恢复

用户离线时可以查看缓存内容并勾选准备项或步骤；恢复网络后操作只同步一次，发生冲突时提示用户处理。

### AC-P0-09：完成与归档

用户确认实验完成后生成执行记录并进入历史；以后编辑方案不会覆盖该次实验引用的方案版本。

### AC-P0-10：晚间汇总

系统按用户时区和设定时间生成当日与次日任务汇总，同一当地日期不会重复生成。

### AC-P0-11：数据导出

用户可以导出符合范围的 JSON 和 CSV；导出只包含当前账户有权访问的数据。

### AC-P0-12：响应式与可访问性

核心闭环在桌面端和移动端均可完成，无关键文字截断、控件遮挡或无法访问的主要操作；键盘焦点和语义标签可用。

## 5. G0 冻结结论

- 产品目标：已冻结。
- P0/P1/P2 边界：已冻结。
- P0 业务规则：已冻结。
- P0 验收基线：已冻结。
- 设计部：允许进入 G1。
- 开发部：允许准备 G2 技术规格，不得越过 G1 直接接入真实数据。
- 测试部：允许准备 G2 测试计划与需求追踪矩阵。

任何影响上述范围、承诺或验收标准的变更，必须由产品部登记后重新评审。

## 6. 部门首轮任务

### 设计部

- 基于本文件与 V1.2 PRD 输出 P0 信息架构、页面清单、核心流程和状态矩阵。
- 覆盖桌面端、移动端、空/加载/错误/离线/冲突/通知不可用状态。
- 在产品部确认流程后再进入具体视觉设计。

### 开发部

- 输出 P0 Tech Spec、API Contract、数据模型、RLS、时间/提醒、离线和 Sites 兼容性验证计划。
- 不开始 P1 文档处理、Worker 或 Docker 工作。
- 在设计事实来源完成前，不开始真实 UI 数据集成。

### 测试部

- 输出 P0 测试计划、需求追踪矩阵、发布阻断标准和重点测试数据。
- 优先覆盖权限隔离、时间、时区、跨午夜、多计时器、Push 降级、离线重放和多设备冲突。

## 7. G2 产品决策补充

以下决策用于关闭测试部在首轮测试设计中提出的 `PD-QA-01` 至 `PD-QA-09`。

### PD-QA-01：提醒延迟验收窗口

- 在时间可控、网络正常的 staging 环境中，到期提醒任务应在目标时间后 2 分钟内被系统领取并形成站内提醒。
- 站内提醒形成后，前台页面应在 60 秒内自动显示，或在用户刷新后立即显示。
- Web Push 到达终端的时间受浏览器、系统和网络控制，不作为“指定分钟必达”的发布承诺；测试应验证发送尝试、失败记录、重试和站内补显链路。

### PD-QA-02：夏令时不存在或重复时间

- 用户设定的当地汇总时间因夏令时跳变而不存在时，在跳变后的第一个有效当地时间生成。
- 当地时间重复出现时，同一用户、当地日期和汇总类型只生成一次。
- 唯一性以用户、当地日期和汇总类型的组合保证，不以单纯 UTC offset 判断。

### PD-QA-03：短时离线时长与缓存目标

- P0 产品目标为连续离线不超过 8 小时。
- 缓存范围为当天和次日的日程、关联方案、准备清单、步骤及已启动计时器事实。
- 离线勾选事件最多保留 24 小时等待同步；超过 24 小时仍未同步时必须提示用户重新联网处理，不得静默丢弃。
- 该目标是 P0 验收范围，不代表永久离线或完整离线编辑能力。

### PD-QA-04：同字段并发冲突

- 服务端保存 `revision`，旧版本写入必须被拒绝。
- 不使用静默的最后写入覆盖。
- 冲突界面展示“服务器当前值”和“本设备待同步值”。
- 用户可以选择保留服务器版本，或基于最新版本重新应用本地操作。
- 勾选类事件在不冲突时按唯一事件 ID 幂等重放。

### PD-QA-05：可访问性标准

- P0 核心流程以 WCAG 2.2 AA 为正式目标。
- 必须覆盖键盘操作、可见焦点、语义标签、表单错误提示、非颜色单一表达、文字对比度和基本屏幕阅读器语义。
- 非核心装饰性问题可按严重度评估，但阻断核心流程的可访问性问题阻断发布。

### PD-QA-06：账户删除与软删除恢复入口

- 两者均属于 P0。
- 设置页提供账户删除入口、影响说明、二次确认和 7 天撤销入口。
- P0 提供“最近删除”入口，用于恢复仍在 30 天保留期内的对象。
- 永久删除属于不可逆重要操作，必须再次确认。

### PD-QA-07：性能与容量目标

- P0 试点容量目标：最多 1,000 个注册账户、100 个并发活跃用户。
- 单个账户的代表性容量：5,000 个日程任务、500 个实验方案、20,000 条步骤或准备项、10,000 条计时和提醒记录。
- 在代表性数据和正常网络环境下，普通已登录页面的数据响应 P95 不超过 1 秒。
- 核心写操作 P95 不超过 1 秒；晚间汇总和提醒领取属于异步任务，按各自时间窗口验收。
- 桌面和中端移动设备的主要页面应在合理网络条件下保持可操作，不因代表性数据量出现明显阻塞。
- 开发部可在 G2 评审中提出成本或平台限制，但不得自行降低目标。

### PD-QA-08：空汇总规则

- 每个启用晚间汇总的用户每天仍生成一份站内汇总。
- 当日和次日均无任务时，显示“今日暂无任务记录，明日暂无安排”。
- 当日和次日均无任务时不发送 Web Push，避免无信息通知。
- 任一日期存在任务时，按用户通知偏好发送 Push，并始终保留站内汇总。

### PD-QA-09：软删除范围与恢复入口

- 可单独进入最近删除并恢复：未完成的日程任务、未被历史执行锁定的实验方案逻辑实体。
- 已完成实验执行记录不得软删除，只能归档和查看。
- 已被实验执行引用的方案版本不得删除；方案逻辑实体可停用，但历史版本必须保留。
- 准备项、实验步骤和计时器不提供独立的最近删除入口，随所属未完成任务一并恢复。
- 站内提醒和晚间汇总不进入最近删除；用户只能标记已读或按产品提供的清理规则处理。

## 8. G1 设计决策补充

以下决策用于关闭设计部 G1 规格中的 `PD-01` 至 `PD-12`。

### PD-01：日程时间冲突

- 冲突只警告，不自动阻止保存。
- 用户必须在冲突摘要中明确确认后才能保存。
- 系统不得自动替用户改期、缩短任务或选择保留哪一个任务。

### PD-02：准备未完成时开始实验

- 允许开始，但必须二次确认。
- 系统记录开始当时未完成准备项的快照。
- P0 不要求用户额外填写自由文本原因。

### PD-03：离线跳过步骤

- 允许离线跳过步骤。
- 跳过原因必填，并与跳过事件一起标记为待同步。
- 原因为空时不得保存跳过操作。

### PD-04：实验完成阻断规则

- 以下情况阻断完成：
  - 存在待同步操作。
  - 存在未解决冲突。
  - 存在仍在运行或暂停但未结束的计时器。
  - 存在未完成且未按规则跳过的必要步骤。
- 可选步骤未处理时只警告，用户确认后可以完成。
- 准备项未全部完成时只警告，因为开始实验时已经保留准备快照。
- 方案作者在编辑步骤时设置“必要/可选”，默认值为“必要”。
- 必要步骤被跳过且填写原因后，视为已处理，但归档记录必须保留“跳过”状态和原因。

### PD-05：空摘要

- 采用 `PD-QA-08` 的冻结规则：保留站内空摘要，不发送无内容 Push。

### PD-06：任务状态转换

- 用户显式操作：开始、暂停当前执行、继续、取消、确认完成、撤销完成。
- 系统推导状态：
  - 待规划：任务未关联已确认方案。
  - 待准备：已关联方案，但准备清单尚未达到全部完成。
  - 待执行：已具备执行条件且尚未开始。
  - 进行中：已开始，且当前没有处于等待中的活动计时器。
  - 等待中：已开始，且至少一个关联计时器正在运行或暂停等待。
  - 已逾期：计划结束时间或所在时段已过，且任务尚未完成或取消。
  - 已完成：用户确认完成。
  - 已取消：用户确认取消。
- 同时满足多个推导条件时，优先级为：已完成/已取消 > 已逾期 > 等待中 > 进行中 > 待执行 > 待准备 > 待规划。

### PD-07：方案待复核

- 手工方案具有草稿、待复核、已确认、已停用状态。
- 新建方案默认为草稿。
- 用户可以将草稿直接确认，也可以主动标记为待复核。
- 待复核方案由用户确认后进入已确认。
- 日程可以暂时关联草稿或待复核方案，但开始实验前必须切换到一个已确认版本。
- 已停用方案不能用于新实验，但不影响历史引用。

### PD-08：误完成恢复

- 用户可在完成后 30 天内执行“撤销完成”。
- 撤销后恢复为完成前的执行状态，并保留原完成时间、撤销时间和操作记录。
- 超过 30 天后不提供普通用户撤销入口。
- 撤销完成不是删除历史；审计记录不得移除。

### PD-09：相似方案处理

- P0 不提供方案字段合并或自动合并。
- 归档时可以提示相似方案，并允许用户查看。
- 用户只能选择继续保留为独立方案，或放弃新建并返回已有方案。
- 任何合并能力进入后续版本重新定义。

### PD-10：任务与方案必填字段

实验任务必填：

- 任务名称。
- 日期。
- 早、中、晚时段。

实验任务选填：

- 精确开始时间和结束时间。
- 关联方案。
- 备注。

实验方案必填：

- 方案名称。
- 至少一个实验步骤。
- 每个步骤的名称或可执行说明。
- 每个步骤的必要/可选属性，默认必要。

实验方案选填：

- 简介、标签和来源说明。
- 器材、试剂、材料和前置准备项。
- 步骤默认时长和计时提醒。
- 注意事项和备注。

### PD-11：软删除范围

- 采用 `PD-QA-09` 的冻结规则。
- 被历史引用的方案逻辑实体只能停用，所有已引用版本必须保留。

### PD-12：账户删除撤销入口

- 用户发起账户删除后立即退出正常业务界面。
- 7 天撤销期内，用户仍可使用原邮箱和密码登录。
- 登录成功后只显示“账户待删除”页面，不加载普通业务首页。
- 用户可以选择撤销删除并恢复正常账户，或退出并维持删除计划。
- 撤销操作需要再次确认，并记录撤销时间。

## 9. G2 技术与发布决策补充

以下决策用于关闭开发部 G2 技术规格提出的 6 项联合评审问题。

### DEV-PD-01：早、中、晚逾期边界

- 所有时间均按任务所属日期及用户当时设置的 IANA 时区计算。
- 早：当地时间 00:00–11:59，未填写精确结束时间时在 12:00 起判定逾期。
- 中：当地时间 12:00–17:59，未填写精确结束时间时在 18:00 起判定逾期。
- 晚：当地时间 18:00–23:59，未填写精确结束时间时在次日 00:00 起判定逾期。
- 用户填写精确结束时间时，以精确结束时间为逾期边界。
- 逾期仅改变显示状态，不自动取消、完成或改期任务。

### DEV-PD-02：生产备份与恢复承诺

- 本地开发和临时测试可以使用满足功能验证的低成本环境。
- 正式生产必须使用能够满足备份、恢复和服务稳定性要求的 Supabase 方案，不以免费层作为生产承诺。
- P0 最低业务目标为 RPO 不超过 24 小时、RTO 不超过 4 小时。
- 正式发布前必须完成一次数据库备份恢复演练并记录实际结果。
- 如果所选 Supabase 方案自身不能满足目标，开发部必须增加独立备份/导出措施；未验证前阻断生产发布。
- P0 不包含用户论文文件，因此本阶段不涉及 P1 Storage 对象备份。

### DEV-PD-03：最近删除数据导出

- JSON 导出包含仍处于 30 天保留期内的软删除对象，并明确输出 `deleted_at`、可恢复截止时间和对象状态。
- CSV 不包含软删除对象，只导出当前有效的任务、方案和执行记录。
- 已永久删除对象不得出现在任何导出中。

### DEV-PD-04：正文格式

- P0 的任务备注、方案简介、步骤说明、跳过原因和一般文本字段统一使用纯文本。
- P0 不支持 Markdown、HTML、富文本、嵌入脚本或自定义样式。
- 换行作为纯文本内容保留。
- 器材、材料、步骤和计时等需要结构化表达的内容使用独立字段或列表，不依赖正文标记语法。

### DEV-PD-05：正式保留期限

- 站内提醒：生成后保留 90 天。
- 晚间汇总：生成后保留 365 天。
- 安全与重要业务审计事件：保留 2 年。
- 数据导出审计：保留 1 年，只记录用户、时间、导出类型、结果和必要追踪 ID，不记录导出正文。
- 账户删除与脱敏处理请求：保留 2 年的最小合规回执，账户业务数据仍按账户删除规则清理。
- 实验执行历史、方案版本和计时事实随账户正常保留，直到账户删除；不得因通知保留期届满而删除实验事实。

### DEV-PD-06：生产认证、Sites M0 与回退规则

- 开发和测试阶段可使用平台测试邮件能力；正式发布必须配置可控的生产邮件发送服务，用于邮箱验证和密码找回。
- Sites 访问模式为公开网站入口；所有个人业务数据必须登录后访问并由 RLS 隔离。
- Sites M0 兼容性验证由开发部主责，测试部提供验收清单，产品部确认业务能力是否满足。
- M0 必须验证：Next.js 构建、SSR、Route Handler、环境变量、Supabase Auth 回调、PWA、Service Worker、Web Push 和自定义域名/HTTPS。
- 任一核心能力无法通过且在当前阶段没有低风险修复方案时，允许按预先批准的部署层回退方案切换到 Vercel。
- 回退不得改变 Supabase 数据模型、业务规则或验收标准。
- Sites M0 与 Vercel 回退预案均属于 P0 发布阻断项，优先级为最高发布准备级别。

## 10. G2 一致性评审产品口径

### G2-QA-01：撤销完成与不可变归档

- 每次“确认完成”都生成一个不可变的完成快照，已经生成的快照不得修改或覆盖。
- 用户在 30 天内撤销完成时：
  - 任务恢复为完成前的执行状态。
  - 原完成快照继续保留，并标记其后发生了撤销事件。
  - 系统追加撤销事件，不删除或改写原完成事实。
- 用户继续执行后再次确认完成时，生成新的完成快照。
- 同一个任务可以因撤销后再次完成而存在多个按时间排序的完成快照，但任一时刻只有最新有效完成状态作为当前状态。
- 知识库默认展示最新有效完成快照；历史页可以查看之前完成、撤销和再次完成的完整时间线。
- “唯一”约束应限定为同一次完成命令/同一个幂等键不得重复生成快照，不得限制一个任务终身只能完成一次。

### G2-QA-02：待同步与跨设备完成阻断边界

- 当前设备存在尚未发送的 outbox 操作时，客户端不得发起完成命令。
- 服务端存在已登记但未解决的同步冲突时，服务端拒绝完成命令。
- 系统无法感知另一台离线设备中尚未发送到服务器的本地操作；P0 不承诺在该操作上传前跨设备预判。
- 如果任务已在其他设备完成，离线设备以后上传的旧 revision 操作必须被拒绝并显示冲突，不得覆盖已完成事实。
- 完成确认界面应提示用户：若其他设备仍离线，稍后同步时可能需要处理冲突。
